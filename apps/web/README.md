# Web Application

This directory is reserved for the future strict TypeScript, React, and Next.js interface. The first
slice will upload an audio file and display duration, sample rate, and channel count.

The UI will be English-only initially while keeping user-facing copy separable for future i18n. No
frontend dependency versions have been selected yet. See `docs/DECISIONS.md`.
