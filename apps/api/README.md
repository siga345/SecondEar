# API Application

This directory is reserved for the future FastAPI transport layer. It will own upload lifecycle,
resource limits, HTTP schemas, configuration, and domain-error mapping. Analytical and decoding logic
belongs in `packages/analysis`.

No API implementation or runtime dependency has been selected yet. See `docs/DECISIONS.md`.
